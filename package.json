{
    "name": "history-manager",
    "version": "1.0.0",
    "description": "Clear Stale History Automatically",
    "main": "build.js",
    "type": "module",
    "scripts": {
        "test": "web-ext lint -s ./dist",
        "build:firefox": "TARGET=firefox vite build",
        "build:chrome": "TARGET=chrome vite build",
        "pack": "web-ext build -s ./dist -o -a ext-out -n history-manager.zip"
    },
    "author": "Matthew_Cash",
    "license": "GPLv3-only",
    "dependencies": {
        "vite": "^4.3.9",
        "vite-plugin-web-extension": "^3.0.9",
        "web-ext": "^7.6.2",
        "webextension-polyfill": "^0.10.0"
    }
}
